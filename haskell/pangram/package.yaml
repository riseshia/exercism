name: pangram
version: 1.0.0.1

dependencies:
  - base
  - containers
  - Cabal

library:
  exposed-modules: Pangram
  source-dirs: src
  dependencies:
  # - bar       # want to use in your solution.

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - pangram
      - hspec
